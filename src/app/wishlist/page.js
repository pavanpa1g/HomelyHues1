"use client";

import BottomNavBar from "@/components/BottomNavBar";
import React from "react";

const Wishlist = () => {
  return <> 
  <p>Wishlist</p>
  <BottomNavBar />
  </>;
};

export default Wishlist;
